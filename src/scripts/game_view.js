export default class GameView {
    constructor(game, ctx) {
        this.game = game;
        this.ctx = ctx;
    }

    keyBindings() {
        addEventListener
    }

    run() {
        this.game.draw(this.ctx);
    }
}